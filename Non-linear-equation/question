The yield y of a coupon with face value F, maturity T, and fixed coupon C1, ….CN payable at times 0 < T1 < … < Tn = T satisfies:
P = [sigma (C*exp(-y*T)]+F*exp(-y*T)
Using Solver03.h, write a program to compute the yield of a coupon bond by solving the above non-linear equation.
